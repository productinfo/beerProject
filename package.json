{
  "name": "project",
  "version": "1.0.0",
  "description": "Honors Project",
  "main": "templates/index.html",
  "scripts": {
    "start": "npm run watch:all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server",
    "watch:all": "parallelshell 'npm run lite'",
    "clean": "rimraf dist",
    "usemin": "usemin index.html -d dist --htmlmin -o dist/index.html && usemin detail.html -d dist --htmlmin -o dist/detail.html",
    "build": "npm run clean && npm run usemin"
  },
  "author": "Joao Schapke",
  "license": "ISC",
  "devDependencies": {
    "cssmin": "^0.4.3",
    "htmlmin": "0.0.7",
    "lite-server": "^2.4.0",
    "onchange": "^5.2.0",
    "parallelshell": "^3.0.2",
    "rimraf": "^2.6.3",
    "uglify-js": "^3.5.2",
    "usemin-cli": "^0.5.1"
  },
  "dependencies": {}
}
